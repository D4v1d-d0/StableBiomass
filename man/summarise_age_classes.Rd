% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarise_age_classes.R
\name{summarise_age_classes}
\alias{summarise_age_classes}
\title{Summarise Biomass and Demography by Age Class}
\usage{
summarise_age_classes(
  biomass_profile,
  age_classes,
  age_col = "age",
  structure_col = "R",
  deaths_col = "D",
  births_col = "B",
  live_biomass_col = "live_biomass",
  mortality_biomass_col = "mortality_biomass",
  birth_biomass_col = "birth_biomass"
)
}
\arguments{
\item{biomass_profile}{A data frame produced by
\code{\link[=derive_biomass_profile]{derive_biomass_profile()}} or another table with equivalent columns.}

\item{age_classes}{A data frame defining age-class intervals. It must
contain \code{age_class}, \code{min_age}, and \code{max_age} columns.}

\item{age_col}{Character string. Name of the age column.
Default is \code{"age"}.}

\item{structure_col}{Character string. Name of the stable structure column.
Default is \code{"R"}.}

\item{deaths_col}{Character string. Name of the deaths column.
Default is \code{"D"}.}

\item{births_col}{Character string. Name of the births column.
Default is \code{"B"}.}

\item{live_biomass_col}{Character string. Name of the live biomass column.
Default is \code{"live_biomass"}.}

\item{mortality_biomass_col}{Character string. Name of the mortality
biomass column. Default is \code{"mortality_biomass"}.}

\item{birth_biomass_col}{Character string. Name of the birth biomass
column. Default is \code{"birth_biomass"}.}
}
\value{
A data frame with one row per age class and columns summarising
demographic structure, deaths, births, live biomass, mortality biomass,
birth biomass, and their relative contributions.
}
\description{
Groups an age-structured biomass profile into user-defined age classes
and computes demographic and biomass totals for each class.
}
\examples{
biomass_profile <- data.frame(
  age = 0:5,
  R = c(1.00, 0.70, 0.45, 0.25, 0.12, 0.05),
  D = c(0.30, 0.25, 0.20, 0.13, 0.07, 0.05),
  B = c(0.00, 0.00, 0.20, 0.40, 0.30, 0.10),
  body_mass = c(5, 20, 45, 70, 80, 82),
  live_biomass = c(5, 14, 20.25, 17.5, 9.6, 4.1),
  mortality_biomass = c(1.5, 5, 9, 9.1, 5.6, 4.1),
  birth_biomass = c(0, 0, 9, 28, 24, 8.2)
)

age_classes <- data.frame(
  age_class = c("juvenile", "subadult", "adult"),
  min_age = c(0, 2, 4),
  max_age = c(1, 3, 5)
)

summarise_age_classes(biomass_profile, age_classes)
}
