% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/derive_biomass_profile.R
\name{derive_biomass_profile}
\alias{derive_biomass_profile}
\title{Derive a Biomass Profile from a Stable Demographic Profile}
\usage{
derive_biomass_profile(
  demographic_profile,
  body_mass,
  age_col = "age",
  structure_col = "R",
  deaths_col = "D",
  births_col = "B",
  mass_col = "body_mass"
)
}
\arguments{
\item{demographic_profile}{A data frame, or a list containing a data frame
named \code{table}. The demographic table must contain age, stable structure,
deaths, and births columns. By default, these columns are \code{age}, \code{R}, \code{D},
and \code{B}.}

\item{body_mass}{A data frame containing age-specific body mass values.
By default, it must contain columns named \code{age} and \code{body_mass}.}

\item{age_col}{Character string. Name of the age column shared by both
input tables. Default is \code{"age"}.}

\item{structure_col}{Character string. Name of the stable structure column.
Default is \code{"R"}.}

\item{deaths_col}{Character string. Name of the deaths column.
Default is \code{"D"}.}

\item{births_col}{Character string. Name of the births column.
Default is \code{"B"}.}

\item{mass_col}{Character string. Name of the body mass column.
Default is \code{"body_mass"}.}
}
\value{
A data frame with age, demographic quantities, body mass, and
derived biomass quantities:
\describe{
\item{live_biomass}{Stable structure multiplied by body mass.}
\item{mortality_biomass}{Deaths multiplied by body mass.}
\item{birth_biomass}{Births multiplied by body mass.}
\item{relative_live_biomass}{Proportion of total live biomass in each age class.}
\item{relative_mortality_biomass}{Proportion of total mortality biomass in each age class.}
}
}
\description{
Combines an age-structured stable demographic profile with body mass
values by age to estimate live biomass, mortality biomass, and birth
biomass across age classes.
}
\examples{
demographic_profile <- data.frame(
  age = 0:4,
  R = c(1.00, 0.70, 0.45, 0.25, 0.10),
  D = c(0.30, 0.25, 0.20, 0.15, 0.10),
  B = c(0.00, 0.00, 0.20, 0.50, 0.30)
)

body_mass <- data.frame(
  age = 0:4,
  body_mass = c(5, 20, 45, 70, 80)
)

derive_biomass_profile(demographic_profile, body_mass)
}
