% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_species_biomass_excel.R
\name{run_species_biomass_excel}
\alias{run_species_biomass_excel}
\title{Run Species Biomass Reconstruction from an Excel File}
\usage{
run_species_biomass_excel(
  input_file,
  output_file = NULL,
  sheet = 1,
  age_classes = NULL,
  overwrite = TRUE
)
}
\arguments{
\item{input_file}{Character string. Path to the input Excel file.}

\item{output_file}{Character string. Path to the output Excel file. When
\code{NULL}, the output file is created next to \code{input_file} with suffix
\verb{_biomass_output.xlsx}.}

\item{sheet}{Sheet name or position passed to \code{\link[readxl:read_excel]{readxl::read_excel()}}.
Default is \code{1}.}

\item{age_classes}{Optional data frame defining age-class intervals, with
columns \code{age_class}, \code{min_age}, and \code{max_age}. When supplied, an
\code{Age_Class_Summary} sheet is added to the output workbook.}

\item{overwrite}{Logical. Controls replacement of an existing output file.
Default is \code{TRUE}.}
}
\value{
Invisibly returns a list containing the output file path, beta
value, demographic profile, biomass profile, biomass summary, and
optional age-class summary.
}
\description{
Reads an Excel file containing age-specific fertility, body mass, and a
single Weibull beta value, reconstructs the stable demographic profile,
derives biomass quantities, and writes the results to a new Excel file.
}
\details{
The input sheet must contain columns named \code{age}, \code{mx}, \code{body_mass}, and
\code{beta}. The \code{beta} column may contain one numeric value and empty cells in
the remaining rows.
}
\examples{
\dontrun{
run_species_biomass_excel(
  input_file = "species_input.xlsx",
  output_file = "species_biomass_output.xlsx"
)
}
}
